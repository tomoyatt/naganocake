<h2>ジャンル編集</h2><br>

    <%= form_with model: @genre, url: admin_genre_path do |f| %>
          <%= f.label :ジャンル %>
          <%= f.text_field :name %>
          <%= f.submit "変更を保存" %>
    <% end %>