<h2>商品説明</h2>

<%= attachment_image_tag @item, :image, :fill, 300, 300, fallback: 'no-image.jpg' %>

<table>
  <tbody>
    <tr>
      <td>商品名</td>
      <td><%= @item.name %></td>
    </tr>
    <tr>
      <td>商品説明</td>
      <td><%= @item.introduction %></td>
    </tr>
    <tr>
      <td><%= (@item.price*1.1).floor.to_s(:delimiter, :unit => "￥") %> (税込み)</td>
    </tr>
  </tbody>
</table>