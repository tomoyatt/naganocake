<table class="table table-bordered ec-genre-list">
  <tbody>
    <tr>
      <td>ジャンル検索</td>
    </tr>
    <% genres.each do |genre| %>
    <tr>
      <td><%= link_to genre.name, genre_path(genre), class:"ec-genre-list-item" %></td>
    </tr>
    <% end %>
  </tbody>
</table>